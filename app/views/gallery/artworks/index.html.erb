<div class="bg-white">
  <div class="container mx-auto px-2 lg:px-0">
    
    <% if @show_landing %>
      <%= render 'gallery_landing' %>
    <% else %>
      <%= render 'category_listing' %>
    <% end %>

  </div>
</div>