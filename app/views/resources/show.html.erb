<% content_for :title, @resource.title %>

<div class="bg-white">
  <div class="container mx-auto px-4 lg:px-0 pt-4 pb-16">
    <% if @resource.category == 'texts' %>
      <%= render 'resources/show_text', resource: @resource %>
    <% else %>
      <%= render 'resources/show_media', resource: @resource %>
    <% end %>
  </div>
</div>
