<% 
  # set defaults 
  artwork ||= @artwork
%>

<div class="bg-gray-100 px-4 md:px-6 pt-4 md:pt-6 pb-6 md:pb-10 mb-8 md:mb-12">
  <h2 class="text-base md:text-lg text-gray-600 underline mb-3 md:mb-4">Collections</h2>
  <ul class="space-y-2 md:space-y-3">
    <% artwork.collections.each do |collection| %>
      <li>
        <%= link_to collection_path(collection.slug),
            class: "block group" do %>
          <% if collection.location.present? %>
            <span class="text-base md:text-lg font-semibold text-gray-600 group-hover:text-blue-400"><%= collection.location %></span>
          <% end %>
          <br>
          <span class="text-lg md:text-xl font-semibold group-hover:text-blue-800"><%= collection.name %></span>
        <% end %>
      </li>
    <% end %>
  </ul>
</div>